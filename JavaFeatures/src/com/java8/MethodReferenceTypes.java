package com.java8;

import java.util.function.Consumer;

/*
 * public class MethodReferenceTypes {
 * 
 * Consumer<String> c = MyClass::printMessage; // static method
 * c.accept("Hello");
 * 
 * MyClass obj = new MyClass(); // instance method Supplier<String> s =
 * obj::getMessage; System.out.println(s.get());
 * 
 * List<String> list = Arrays.asList("Java", "Spring", "Lambda"); // Arbitory
 * method reference list.forEach(System.out::println);
 * 
 * Supplier<Student> sup = Student::new; // constructor Student st = sup.get();
 * }
 */